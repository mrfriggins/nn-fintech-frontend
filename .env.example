# Base URL of the NN-Fintech backend API. Use an https:// origin in production.
NEXT_PUBLIC_API_URL=http://localhost:8080

# PayPal JS SDK client id (public identifier, not a secret).
# Payment UI is hidden when this is unset.
NEXT_PUBLIC_PAYPAL_CLIENT_ID=
